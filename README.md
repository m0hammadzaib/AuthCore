# AuthCore
AuthCore is a secure and scalable authentication &amp; user management API built with Node.js, Express, and PostgreSQL. It features JWT-based authentication, role-based access control (user/admin), password hashing, protected routes, and structured error handling — designed with production-level backend architecture in mind.
